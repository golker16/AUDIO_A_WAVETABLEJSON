#!/usr/bin/env python3
# Central entrypoint.
#
# NOTE: I couldn't overwrite the uploaded app.py file in this environment (read-only),
# so this file is the "thin app.py" equivalent. In your local project you can rename
# this to app.py.

from app3 import main


if __name__ == "__main__":
    main()
